name: 'Hello World'

#inputs:
#  who-to-greet:  # id of input
#    description: 'Who to greet'
#    required: true
#    default: 'World'
outputs:
  jojo:
#    description: "Random number"
    value: ${{ sercets.JO_JO }}
runs:
  using: "composite"
  steps:
    - run: echo Hello ${{ sercets.JO_JO }}.
      shell: bash
